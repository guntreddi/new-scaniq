
import React from 'react';

// This component is currently unused as Hero.tsx uses PlatformIllustration, 
// but we export a null component to prevent build errors if the file is referenced.
export const HeroIllustration: React.FC = () => {
  return null;
};
